sudo: false
notifications:
  email: false
language: node_js
node_js:
  - "10"
install:
  - npm install -g npm@2
script:
  - npm run build
deploy:
  provider: surge
  project: ./dist
  domain: https://nbenakis.surge.sh/
  skip-cleanup: true
  on:
    branch: master
